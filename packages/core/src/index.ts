// blocks-core — registry schema, content hashing, and shared helpers.
// Public surface is intentionally empty for now; primitives land here as the
// registry takes shape (see ARCHITECTURE.md).

export const CORE_VERSION = '0.0.0';
