import { GettingStarted } from '~/components/views/getting-started';

export const metadata = {
  title: 'Prise en main — ibirdui blocks',
  description:
    'Installez et composez les blocks ibirdui : prérequis, ajout d’un block via la CLI, thème et tokens, puis un premier exemple d’usage.',
};

export default function Page() {
  return <GettingStarted />;
}
