import { Templates } from '~/components/views/templates';

export const metadata = {
  title: 'Templates — ibirdui blocks',
  description:
    'Des pages complètes composées de blocks ibirdui : landing SaaS, pricing, dashboard, auth. Une commande installe la page, ses blocks et leurs primitives.',
};

export default function Page() {
  return <Templates />;
}
