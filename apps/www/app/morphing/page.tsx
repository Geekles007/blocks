import { MorphingView } from '~/components/morphing/morphing-view';

export const metadata = {
  title: 'Morphing UI — ibirdui blocks',
  description:
    'Dix composants premium où un objet se transforme en un autre — des transitions shared-element construites sur les primitives ibirdui.',
};

export default function Page() {
  return <MorphingView />;
}
