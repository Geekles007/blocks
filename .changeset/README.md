# Changesets

This folder is managed by [Changesets](https://github.com/changesets/changesets).

Run `pnpm changeset` to record a version bump for `blocks-core` and `blocks`,
then `pnpm version-packages` and `pnpm release` to publish.
